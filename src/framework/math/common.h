#ifndef __FRAMEWORK_MATH_COMMON_H_INCLUDED__
#define __FRAMEWORK_MATH_COMMON_H_INCLUDED__

const float PI = 3.141593f;             // 180 degrees
const float HALF_PI = PI / 2.0f;        // 90 degrees
const float QUARTER_PI = PI / 4.0f;     // 45 degrees
const float TWO_PI = PI * 2.0f;         // 360 degrees

const float RADIANS_0 = 0.0f;
const float RADIANS_45 = PI / 4.0f;
const float RADIANS_90 = PI / 2.0f;
const float RADIANS_135 = (3.0f * PI) / 4.0f;
const float RADIANS_180 = PI;
const float RADIANS_225 = (5.0f * PI) / 4.0f;
const float RADIANS_270 = (3.0f * PI) / 2.0f;
const float RADIANS_315 = (7.0f * PI) / 4.0f;
const float RADIANS_360 = PI * 2.0f;

const float PI_OVER_180 = PI / 180.0f;

const float TOLERANCE = 0.00001f;
const float EPSILON = 0.0005f;

#endif
